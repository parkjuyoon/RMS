<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.ktds.targetai.mapper.ApiMapper">

	<!-- 서비스 리스트 조회 -->
	<select id="getActiveList" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT 
		    *
		FROM
			targetai.EV_CUST_ITEM1_TXN CUST
		    LEFT JOIN targetai.EV_CONT_ITEM1_TXN CONT ON (CUST.SRC_CUST_SORC_ID = CONT.SRC_CUST_SORC_ID)
		WHERE
			CUST.SRC_CUST_SORC_ID = #{val}
	</select>
	
	<select id="getPkgBySvcId" parameterType="string" resultType="java.util.HashMap">
		SELECT
			P.PKG_ID,
			P.PATH,
			P.PKG_NM,
			P.DRL_NM,
			S.NUM_OF_OFFER,
    		S.TARGET_TYPE
		FROM
			targetai.SVC S
			LEFT JOIN targetai.PKG P ON (S.PKG_ID = P.PKG_ID)
		WHERE
			S.SVC_ID = #{svc_id}
	</select>
	
	<insert id="addSvclogIn" parameterType="java.util.HashMap">
		INSERT INTO targetai.SVCLOG (
			IO_FLAG,
			SVC_ID,
			PARAM,
			VAL
		) VALUES (
			"IN",
			#{SVC_ID},
			#{PARAM},
			#{VAL}
		)
	<selectKey order="AFTER" keyProperty="SVCLOG_ID" resultType="INT">
		SELECT LAST_INSERT_ID()
	</selectKey>
	</insert>
	
	<insert id="addSvclogOut" parameterType="java.util.HashMap">
		INSERT INTO targetai.SVCLOG (
			SVCLOG_ID,
			IO_FLAG,
			SVC_ID,
			PARAM,
			VAL,
			RULE_ID
		) VALUES 
		<foreach collection="RESULT" separator="," item="res">
		(
			#{SVCLOG_ID},
			"OUT",
			#{SVC_ID},
			#{PARAM},
			#{VAL},
			#{res.RULE_ID}
		)
		</foreach>
	</insert>
	
	<update id="updateRspnsCd" parameterType="java.util.HashMap">
		UPDATE targetai.SVCLOG
		SET
			RSPNS_CD = #{RSPNS_CD}
		WHERE
			SVCLOG_ID = #{TRANSACTION_ID}
			AND RULE_ID = #{RULE_ID}
	</update>
	
	<!-- 서비스에 연결되어 있는 RULE 아이디 리스트 조회 -->
	<select id="getRuleIdsBySvcId" parameterType="String" resultType="int">
		SELECT
		    R.RULE_ID
		FROM
			targetai.SVC S
			LEFT JOIN targetai.RULE R ON (R.PKG_ID = S.PKG_ID)
		WHERE
			S.SVC_ID = #{svc_id}
		ORDER BY R.SALIENCE ASC, R.RULE_ID ASC
	</select>
	
	<!--  -->
	<select id="getFunctionByPkgId" parameterType="int" resultType="java.util.HashMap">
		SELECT 
			RF.RULE_FUNC_ID,
		    RF.RULE_ID,
		    RF.FUNC_NM,
		    RF.FUNC_FLAG,
		    RFA.ARG_VAL,
		    FF.`ORDER`,
		    FF.DATA_TYPE
		FROM
			targetai.RULE_FUNC_ARGS RFA
		    LEFT JOIN targetai.FACTOR_FUNC FF ON (RFA.FACTOR_FUNC_ID = FF.FACTOR_FUNC_ID)
		    LEFT JOIN targetai.RULE_FUNC RF ON (RFA.RULE_FUNC_ID = RF.RULE_FUNC_ID)
			LEFT JOIN targetai.RULE R ON (RF.RULE_ID = R.RULE_ID)
		WHERE
			R.PKG_ID =	#{pkgId}
	</select>
	
</mapper>