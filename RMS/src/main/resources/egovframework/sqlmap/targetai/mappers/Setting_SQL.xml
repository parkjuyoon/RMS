<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.ktds.targetai.mapper.SettingMapper">
	
	<select id="getFuncList" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT
			FACTOR_ID,
			FACTOR_NM,
			FACTOR_NM_EN,
			FACTOR_TYPE,
			DATA_TYPE,
			NUM_OF_GRP,
			CATEGORY_YN,
			FACTOR_GRP_ID,
			ATTR_USE_YN
		FROM
			targetai.FACTOR
		WHERE
			FACTOR_TYPE = 'FUNC'
			AND ATTR_USE_YN = "Y"
	</select>
	
	<select id="getParamInfo" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT
			FACTOR_FUNC_ARGS_ID,
			FACTOR_ID,
			ARG_NM,
			`ORDER`,
			DATA_TYPE
		FROM
			targetai.FACTOR_FUNC_ARGS
		WHERE
			FACTOR_ID = #{factorId}
		ORDER BY `ORDER` ASC
	</select>
	
	<select id="getSourceInfo" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT
			FACTOR_FUNC_SOURCE_ID,
			FACTOR_ID,
			SOURCE_CODE,
			REG_USRID
		FROM
			targetai.FACTOR_FUNC_SOURCE
		WHERE
			FACTOR_ID = #{factorId}
	</select>
	
	<select id="getFactorById" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT
			FG.FACTOR_GRP_ID,
		    FG.FACTOR_GRP_NM,
		    F.FACTOR_ID,
		    F.FACTOR_NM,
		    F.FACTOR_NM_EN,
		    F.FACTOR_TYPE,
		    F.DATA_TYPE,
		    F.ATTR_USE_YN
		FROM
		    targetai.FACTOR F, targetai.FACTOR_GRP FG
		WHERE
			F.FACTOR_GRP_ID = FG.FACTOR_GRP_ID
		    AND F.ATTR_USE_YN = 'Y'
		    AND F.FACTOR_ID = #{factorId}
	</select>
 
 	<insert id="addFunctionFactor" parameterType="java.util.HashMap">
 		INSERT INTO targetai.FACTOR
 	</insert>
 	
 	<insert id="addFunctionParameter" parameterType="java.util.HashMap">
 		INSERT INTO targetai.FACTOR
 	</insert>
 	
 	<insert id="addFunctionSource" parameterType="java.util.HashMap">
 		INSERT INTO targetai.FACTOR
 	</insert>
 	
 	<update id="updateFunctionFactor" parameterType="java.util.HashMap">
 	
 	</update>
 	
 	<update id="updateFunctionParameter" parameterType="java.util.HashMap">
 	
 	</update>
 	
 	<update id="updateFunctionSource" parameterType="java.util.HashMap">
 	
 	</update>
 	
</mapper>